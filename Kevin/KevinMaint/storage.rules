// Firebase Storage starter rules
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /issue-photos/{issueId}/{allPaths=**} {
      allow read, write: if request.auth != null;
      // TODO: tighten with role-based policies
    }
  }
}
